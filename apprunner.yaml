version: 1.0
runtime: python311
build:
  commands:
    build:
      - pip3 install -r requirements.txt
    post-build:
      - python3 manage.py test
run:
  runtime-version: 3.11
  # command: sh startup.sh
  command: gunicorn --workers 2 dj_app_runner.wsgi --log-file -
  network:
    port: 8000
